export class Peso_Animal {
  constructor(
    id,
    animal_id,
    peso,
    data_cadastro,
    ativo
  ) {
    this.id = id;
    this.animal_id = animal_id;
    this.peso = peso;
    this.data_cadastro = data_cadastro;
    this.ativo = ativo;
  }
}